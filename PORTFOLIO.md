# 우리웨딩 노트 — 결혼 준비 공유 다이어리

예비부부가 결혼 준비 전 과정(체크리스트·예산·웨딩홀 비교·업체 컨택·하객 관리·상견례·신혼여행·본식 당일 진행)을 한 커플 계정으로 함께 관리하는 웹 서비스입니다. 1인 개발로 기획부터 백엔드/프런트엔드 구현, DB 설계, 배포까지 전 과정을 진행했습니다.

- **배포**: 프런트엔드 [Vercel](https://vercel.com) · 백엔드 [Render](https://render.com) · DB [Neon](https://neon.tech) (Serverless Postgres)
- **저장소**: 모노레포 (`frontend/`, `backend/` 분리 배포)

## 왜 만들었나

결혼 준비는 신랑·신부가 각자 다른 곳에서 같은 정보를 따로 찾고, 예산이나 체크리스트를 카톡·엑셀로 주고받으며 관리하게 되는 경우가 많습니다. 이 앱은 "커플 = 하나의 계정 공간"을 기본 단위로 삼아, 두 사람이 같은 데이터를 실시간으로 같이 보고 수정하는 것을 목표로 만들었습니다.

## 핵심 기능

| 영역 | 내용 |
|---|---|
| 커플 연결 | 회원가입 후 커플 생성 시 6자리 초대 코드 발급 → 배우자가 코드 입력으로 참여. 둘 다 각자 커플을 만들었을 경우 한쪽 것을 버리고 상대 코드로 갈아탈 수 있음 |
| 체크리스트 | 카테고리별 결혼 준비 항목, 담당자(신랑/신부/공동) 지정, 공동 항목은 양쪽 메모를 분리 저장, 목표일 관리 |
| 예산관리 | 총예산 설정 → 카테고리별 권장 배분(프리셋 비율) 자동 계산, 항목별 계약금/잔금/할인 관리, 결제수단·부담자별 정산 리포트 |
| 웨딩홀 가이드 | 후보 웨딩홀 비교(대관료·식대·보증인원 구조화 필드로 총금액 자동 계산), 지인에게 받은 참고 견적 별도 기록, 체크리스트형 투어 메모 |
| 업체컨택 | 스튜디오·드레스·메이크업 등 카테고리별 업체 관리, 계약 상태 추적, 카테고리별 확인 체크리스트 |
| 상견례 | 신랑·신부 거주 지역 입력 시 중간지점 추천, 사전/당일 체크리스트 |
| 신혼여행 | 목적지별 예산 계산기, 항공편·숙소 실제 예약 정보(다구간 지원) |
| 하객관리 | 신랑측/신부측 자동 분리(로그인 계정 기준이라 PIN 없이도 상대측 목록 비노출), RSVP·식수 집계, 청첩장 링크 |
| 웨딩 당일 | 사회자 큐시트(시간·소요시간·담당·멘트), 축의금 관리, 혼인서약서 |
| 커뮤니티 | 웨딩홀 후기/상견례 장소 후기 공개 게시판, 신고 기능 |
| 관리자 | 공지사항(배너/팝업), 신고 처리, 회원 정지, 문의 답변, **가이드 콘텐츠 관리**(체크리스트 문구·지역 데이터 등을 코드 배포 없이 DB에서 직접 수정) |

## 기술 스택

**Frontend**: React 19 + Vite, React Router 7 (client-side routing), Context API로 전역 상태 관리(인증/커플/가이드 콘텐츠), 순수 CSS(커스텀 프로퍼티 기반 다크모드), 별도 상태관리 라이브러리 없이 Context + 로컬 fetch 패턴으로 단순하게 유지.

**Backend**: Node.js + Express (ESM), PostgreSQL을 ORM 없이 `pg`로 직접 쿼리, `node-pg-migrate`로 스키마 버전 관리, JWT 기반 인증(access + refresh 이원화), `bcrypt`(v6) 비밀번호 해싱, `express-rate-limit`으로 구간별 요청 제한.

**Infra**: Vercel(프런트, 정적 SPA) / Render(백엔드 API 서버) / Neon(서버리스 Postgres) — 프런트·백엔드·DB가 서로 다른 3개 플랫폼에 흩어진 구성이라, CORS·쿠키 SameSite·환경변수 관리가 실제 신경 써야 할 지점이었습니다(아래 트러블슈팅 참고).

## 시스템 아키텍처

```
[Vercel]                [Render]                 [Neon]
React SPA  --HTTPS-->   Express API  --SSL-->     Postgres
(정적 빌드)              (couple_id 스코프         (node-pg-migrate로
                          라우팅, JWT 검증)          스키마 관리)
```

- **인증 흐름**: access token은 15분짜리 JWT를 프런트 메모리(모듈 변수)에만 보관(XSS로 인한 탈취 위험 축소, localStorage 미사용), refresh token은 30일짜리 JWT를 httpOnly + `SameSite=None; Secure` 쿠키로 저장. API 요청이 401을 받으면 클라이언트가 자동으로 `/auth/refresh`를 호출해 재발급받고 원래 요청을 재시도 — 동시에 여러 요청이 401을 받아도 refresh 호출은 하나만 나가도록 in-flight promise를 공유.
- **데이터 모델의 핵심 단위는 `user`가 아니라 `couple`**: 체크리스트·예산·웨딩홀 등 거의 모든 도메인 테이블이 `couple_id`로 스코프됨. 요청마다 `groom_user_id OR bride_user_id = 현재 로그인 유저`로 커플을 찾아 권한을 확인하는 공통 미들웨어 패턴을 사용해, 상대방 데이터를 실수로 노출하는 라우트가 생기지 않도록 통일.
- **가이드 콘텐츠 분리**: 체크리스트 기본 문구, 지역 그룹, 예산 프리셋 비율 같은 "정적이지만 자주 손볼 수 있는" 데이터를 코드에 하드코딩하지 않고 `guide_content` 테이블에 JSON으로 저장 → 관리자 페이지에서 수정하면 재배포 없이 즉시 반영.

## 주요 설계 결정과 이유

- **ORM 대신 raw SQL + node-pg-migrate**: 쿼리가 복잡하지 않고(단일 테이블 CRUD 위주) 스키마가 도메인마다 명확히 분리돼있어, ORM의 추상화 비용보다 SQL을 직접 보는 게 디버깅에 유리하다고 판단. 다만 스키마 변경 이력 관리는 필요해서 마이그레이션 도구는 도입(처음엔 `schema.sql` 파일 하나를 수동으로 실행하는 방식이었다가, 배포 환경마다 손으로 맞춰야 하는 문제를 겪고 `node-pg-migrate`로 전환).
- **커플 슬롯이 하나만 찬 상태를 정상 상태로 취급**: 배우자가 먼저 가입 안 해도 앱을 바로 쓸 수 있게 하고(초대 코드 발급 즉시 대시보드 진입), 두 사람이 각자 커플을 만들어버린 경우를 대비해 한쪽을 버리고 상대 코드로 합류하는 기능(`POST /couples/switch`)을 트랜잭션으로 처리 — 삭제와 참여가 한 번에 성공/실패하도록 보장.
- **DATE 컬럼 커스텀 파서**: Postgres DATE를 JS Date로 자동 변환하면 로컬 타임존 계산 과정에서 날짜가 하루 밀려 보이는 문제가 있어, `pg.types.setTypeParser`로 `'YYYY-MM-DD'` 문자열 그대로 받아 프런트에서 문자열로만 다루도록 통일.

## 트러블슈팅

배포 과정에서 실제로 겪고 해결한 문제들입니다.

- **CORS preflight 실패 — 값은 맞는데 안 됨**: Render의 `FRONTEND_ORIGIN` 환경변수 끝에 슬래시(`/`)가 하나 더 붙어있어서 `Access-Control-Allow-Origin` 응답 헤더가 실제 origin과 문자열 단위로 불일치 → 브라우저가 조용히 요청을 막음. 에러 메시지에 두 값이 나란히 찍혀서 육안 비교로 원인 확인.
- **프로덕션에서 계속 `localhost:4000`을 호출함**: Vite는 `VITE_API_URL` 같은 `import.meta.env` 값을 **빌드 시점에 번들에 박아넣기** 때문에, Vercel에 환경변수만 추가하고 재배포를 안 하면 이전 빌드가 그대로 서빙되어 로컬 주소를 계속 호출. 환경변수 추가 후 재배포가 필수라는 걸 배포 로그로 확인.
- **클라이언트 라우팅 새로고침 404**: `react-router-dom`의 `BrowserRouter`는 실제 URL 경로(`/login`, `/register`)를 쓰는데, Vercel은 기본적으로 정적 파일 매칭만 하므로 그 경로로 직접 새로고침하면 404. `vercel.json`에 모든 경로를 `index.html`로 rewrite하는 규칙을 추가해 해결.
- **빈 DB에서 "불러오는 중..."이 영원히 멈춤**: 배포 초기 Neon DB에 스키마만 있고 `guide_content` 시드 데이터가 없는 상태에서, 이 데이터에 의존하는 페이지들이 `!data || !guideValue`로 로딩을 게이팅하고 있었는데, `guideValue`는 "로딩 중"과 "그 키가 원래 없음"을 구분하지 못하는 구조라 후자의 경우 스피너가 무한히 도는 버그였음. `useGuideContentReady()`(최초 fetch 완료 여부)를 별도로 추적해 "로딩 완료됐는데 값이 없음"을 에러 상태로 명확히 분리.
- **`npm audit` 취약점 6건(치명적 1건 포함)**: 실제로는 `bcrypt`가 네이티브 바이너리를 빌드할 때만 쓰는 `node-pre-gyp` 체인(`tar`, `glob`, `rimraf`)의 구버전 문제였고 런타임 요청 경로와는 무관했지만, `bcrypt` 6.x로 업그레이드해 정리(API 호환이라 코드 변경 없음).

## 향후 개선 예정

- 신혼여행 탭 API·화면 마무리
